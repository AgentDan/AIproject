{
  "name": "ai-product-scene-platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-driven platform for working with 3D and product scenes.",
  "workspaces": [
    "apps/client",
    "apps/mobile",
    "apps/server",
    "packages/ai",
    "packages/contracts"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace apps/server\" \"npm run dev --workspace apps/client\"",
    "dev:server": "npm run dev --workspace apps/server",
    "dev:client": "npm run dev --workspace apps/client",
    "dev:mobile": "npm run start --workspace apps/mobile",
    "build": "npm run build --workspace apps/client",
    "build:ai": "npm run build --workspace @ai-product-scene-platform/ai",
    "build:deploy": "npm run build:ai && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  }
}
