{
  "name": "ai-product-scene-platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-driven platform for working with 3D and product scenes.",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --workspace apps/server\" \"npm run dev --workspace apps/web\"",
    "dev:server": "npm run dev --workspace apps/server",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  }
}
