# Client layer (схема v2)

| Каталог | Назначение |
|---------|------------|
| `shell/` | UI shell — оболочка приложения (`AppShell.jsx`) |
| `widgets/` | Domain UI widgets — 3D preview, command input, result viewer |
| `api/` | HTTP-клиент к Platform API |

Дорожная карта: `shell/auth/`, `shell/chat/`, `realtime/` (WebSocket · push).
